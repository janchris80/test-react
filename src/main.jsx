import React from "react";
import ReactDOM from "react-dom/client";
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <h1>Hello from React (auto-deploy) (deployed via deployer)</h1>
  </React.StrictMode>
);
